<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>CRUD nodejs</title>
  
     <link rel="stylesheet"   href="../public/bootstrap/css/bootstrap.css" type="text/css">
    <link rel="stylesheet" href="../public/css/pacientes.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/2.0.2/anime.min.js"></script>
</head>
<body>
    <nav class="navbar navbar-dark bg-dark mb-4" style="z-index: 2;">
        <a href="/" class="navbar-brand" style="font-size: 25px; ">BD Gane Salud Oral</a>
        <div class="rectangulo_navbar"></div>
    </nav>
            <h2 style="color:#0F037E; font-size: 30px;">Historial de Pacientes</h2>
       
    <div class="container">
       
        <div class="row">
            <div class="col-lg-12">
                <table class="table">
                    <thead  class="thead-dark">
                        <tr>
                            <th>CC</th>
                            <th>Nombres</th>
                            <th>Apellidos</th>
                            <th >Acción</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for(var i= 0;  i<tasks.length; i++) { %>
                            <tr class="info_paciente">
                      <!--para interpretar una variable-->
                                <td><%= tasks[i].documento %></td>
                                <td ><%= tasks[i].nombres %></td style="margin-left: 2rem;">


                                        <td ><%= tasks[i].apellidos %></td>
                                        
                                <td >
                                  
                                    <div class="botones">
                                <a href="/historiacli/<%= tasks[i]._id %>" class="btn btn-primary">Abrir</a>
                                <a href="/edit/<%= tasks[i]._id %>" class="btn btn-secondary">Editar</a>
                                <a href="/delete/<%= tasks[i]._id %>" class="btn btn-danger" id="eliminar" >Eliminar</a>
                                    </div>
                                </td>

                            </tr>
                            <%}%>
                           

                            
                        <script>
                       document.getElementById("eliminar").addEventListener("click", eliminarUsuario);

                       function eliminarUsuario() {
                       var eliminar = confirm('Está seguro que quiere eliminarlo?');
                       if(eliminar == true){
                        
                       }else{
                        document.getElementById("eliminar").href=""
                       }
                       }
                        </script>
                    </tbody>
                </table>
            </div>           
        </div>
    </div>
        
    </body>
</html>